// Withdraw → "Add bank account" lands here. Same screen as the KYC bank
// step so the 5-account cap and the OTP gate apply on every entry point.
export { BankFormScreen as default } from "@features/wallet/screens/BankFormScreen";
